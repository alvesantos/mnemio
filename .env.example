# http://localhost:8000 funciona só no navegador do PC.
# Testando no celular físico (Expo Go), usa o IP local da máquina:
# roda `hostname -I` no backend e usa http://<ip>:8000
EXPO_PUBLIC_API_URL=http://localhost:8000
